# Mirrors pyproject.toml so the simple Dockerfile (`pip install -r requirements.txt`)
# resolves the same dependency set as the uv-based Dockerfile.prod.
# Source of truth is pyproject.toml — keep these in sync.

# Core API
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0
python-dotenv>=1.0.1
pyyaml>=6.0.1

# OpenAI + Agents SDK
openai>=1.51.0
openai-agents>=0.0.9
tiktoken>=0.7.0

# Vector store
qdrant-client>=1.12.0

# Persistence + auth
sqlalchemy[asyncio]>=2.0.36
asyncpg>=0.30.0
alembic>=1.13.3
redis>=5.2.1
python-jose[cryptography]>=3.3.0

# Notifications
resend>=2.0.0

# Observability
prometheus-client>=0.21.0

# Dev/test (kept here so Render's image can also run pytest if desired)
pytest>=8.2.0
pytest-asyncio>=0.23.0
fakeredis>=2.26.2
